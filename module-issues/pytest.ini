[pytest]
# Konfiguracja pytest dla modułu Issues
testpaths = tests
# Dodaj katalog modułu do sys.path aby importy działały bez instalacji pakietu
pythonpath = .
# Wyświetlaj pełne traceback dla nieudanych testów
addopts = -v --tb=short
